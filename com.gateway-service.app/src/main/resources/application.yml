server:
  port: 9191

spring:
  application:
    name: gateway-service
  config:
    import: configserver:http://${CONFIG_SERVER_URL:localhost}:9090

logging:
  level:
    org.springframework.cloud.gateway: TRACE
    org.springframework.boot.context.config: DEBUG
